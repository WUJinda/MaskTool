# mask-tool 使用与测试指南（简明版）

> 适用版本：2026-09-18 本地部署 · `E:\Project\mask-tool`
> 能力：Word(.docx) / Excel(.xlsx) 内容脱敏 + 文件名/目录名脱敏，可逆；PPT/PDF 拒绝处理。

## 一、两种用法

### 用法 A：命令行（推荐批量处理）

打开 cmd，进入项目并激活环境（每次新开终端都要执行）：

```bat
cd /d E:\Project\mask-tool
.venv\Scripts\activate
```

**场景 1：整个目录脱敏（最常用）**——文档内容 + 文件名 + 目录名一次完成：

```bat
mask-tool mask D:\待交付资料 --mode smart --all --output D:\脱敏输出
```

产物在 `D:\脱敏输出\<时间戳批次>\` 下：
- `source\` = 脱敏后的完整目录树（可直接交付外部）
- `mapping.json` = 还原钥匙（含全部映射，**妥善保管，勿随脱敏文件一起外发**）
- `report.json` = 处理报告；`skipped_unmasked\` = 未勾选/未确认的文件隔离区（内含未脱敏原件，勿外发）

**场景 2：单文件脱敏**（文件名也会脱敏，产物带 `_masked` 后缀）：

```bat
mask-tool mask D:\合同-某某公司.docx --mode smart --output D:\脱敏输出
```

**场景 3：还原**（把脱敏目录/文件变回原样）：

```bat
mask-tool unmask "D:\脱敏输出\<批次>\source" --mapping "D:\脱敏输出\<批次>\mapping.json" --output D:\还原结果
```

**场景 4：先看看会脱什么（不动文件）**：

```bat
mask-tool inspect D:\合同.docx
```

**模式说明**：
| 命令 | 效果 |
|---|---|
| （默认） | 只脱"高置信自动项"：词库命中、手机号、身份证号 |
| `--all` | 连建议项一起脱：金额、邮箱、银行卡、大整数卡号等 |
| `--confirm` | 逐项弹出确认表，勾选后处理 |
| `--irreversible` | 不可逆模式（`***`），不需要还原时用 |

**常见问题**：控制台输出乱码 → 先执行 `set PYTHONIOENCODING=utf-8`；配置词库在 `config\lexicon.yaml`（把自己常出现的公司名、人名、项目名加进去，脱敏更准）。

### 用法 B：桌面窗口（适合单文件、不熟命令行）

双击项目根目录的 `start-windows.bat`（或在激活环境后运行 `mask-tool app`）。

弹出原生桌面窗口：上传 docx/xlsx → 查看检测表格 → 勾选要脱敏的项（或全选）→ 脱敏下载（弹原生保存对话框）；支持用 mapping.json 反向还原。关闭窗口即完全退出，不会残留后台服务。

> 注：旧版浏览器页面入口（`mask-tool-web` / `streamlit run`）已下线；窗口内界面即原 Web 界面，操作方式不变。

**指定你自己关注的敏感词（针对误报多的场景）**：上传文件后、点"开始检测"前，检测页有两个控件：

1. **临时自定义敏感词**文本框：每行一个词，或用逗号分隔（如 `某某科技有限公司，张三丰，2026年Q3财报`）。这些词会按词库同样规则精确匹配（置信度 0.95，自动脱敏档），在检测表格里置顶并标"✍️ 手动"；与自动检测命中同一个词时只保留一条（以手动为准）。仅本次任务生效，不写入词库文件，点"重新检测"后仍保留。
2. **"仅脱敏我指定的词"开关**（默认关）：
   - 关 = 合并模式：手动词 + 自动检测（词典/NER/正则）结果一起展示，勾选后正常脱敏；
   - 开 = 仅手动模式：完全跳过自动检测（适合 NER 老把普通词当人名这类误报场景），表格里只看你指定的词，脱敏也只处理这些词。

提示：自动检测里误报的词（如"银行"被判成人名），可以加进 `config\whitelist.yaml` 的 whitelist 永久排除（输入框下方也有此提示）。两个控件留空/关闭时，工具行为与原来完全一样。

**中途换文件 / 重新开始**：随时点侧边栏顶部"🔄 新建任务"放弃当前流程重新开始（已上传文件、检测结果、勾选全部清空，但设置和上面输入的自定义敏感词保留）；检测后想换文件点"⬅️ 重新上传文件"；脱敏完成后点"🆕 开始新任务"开始下一批。

**整批目录脱敏（zip）**：把目录压成 zip，在上传区下方的"或上传目录压缩包（.zip）"入口上传。工具递归处理里面的 docx/xlsx（自定义敏感词、仅手动模式照常生效），pptx/pdf 会警告且不进产物；完成后下载同结构目录的 zip。mapping.json 有单独的下载按钮——它是还原钥匙，和脱敏 zip 分开保管，不要一起外发。恢复时在"恢复还原"页上传脱敏 zip + mapping.json，还原为同结构 zip。上传单文件时直接下载脱敏后的同名文件（不再强制打 zip）。

## 二、如何测试这个工具

### 1. 自动化测试（30 秒，验证代码本身没坏）

```bat
cd /d E:\Project\mask-tool
.venv\Scripts\python -m pytest -q
```
预期最后一行：`233 passed, 5 skipped`（5 个 skip 是标记的基线项，非故障）。

### 2. 冒烟测试（5 分钟，用你自己的真实文件走全流程）

拿 1-2 个真实文档（含公司名/人名/手机号的 docx 或 xlsx 最好），放进一个测试文件夹：

```bat
mkdir D:\masktest\src
:: 把文件拷进去，然后：
mask-tool mask D:\masktest\src --mode smart --all --output D:\masktest\out
```

**验收检查 4 点**：
1. 打开 `out\<批次>\source\` 里的文件：敏感词全部变成 `[COMPANY_001]` 之类占位符？文件名/文件夹名也脱了？
2. 用 Word/Excel 打开产物确认正常可读（表格、格式、公式没坏）
3. 还原：`mask-tool unmask "D:\masktest\out\<批次>\source" --mapping "D:\masktest\out\<批次>\mapping.json" --output D:\masktest\back` → 打开 `D:\masktest\back\` 里的文件，与原件逐字一致？
4. 重复跑一次 mask 同一目录 → 生成新批次，旧批次没被覆盖？

### 3. 专项测试点（按你的使用场景挑着测）

- Excel 金额/卡号：造一个含 `¥1,200,000.00` 货币格式和 16-19 位数字的 xlsx，`--all` 模式跑，确认替换且还原后数值可参与计算
- 深目录：嵌套多层中文目录名含敏感词，确认目录名逐级脱敏且还原后结构一致
- 边界文件：把 .pptx/.pdf 混进目录 → 工具应明确拒绝且产物树里没有它们
- 空 xlsx / 加密 xlsx → 应报错跳过而非崩溃

## 三、注意事项（交付外部前必读）

1. **mapping.json 是"钥匙"**：明文存有原文↔占位符对照，和脱敏产物分开存放，绝不外发
2. **docx 修订记录不处理**：交付前先在 Word 里"接受所有修订"再脱敏
3. PPT/PDF 会被拒绝处理且不进产物树——这类文件先自行处理（转成 docx 或手工脱敏）
4. 批次目录（`<时间戳>`）每次运行新建，不覆盖历史，定期清理即可
5. 线程化的 xlsx 批注会丢失（工具会预先警告）
